= simple_form_for(@participant) do |f|
  = f.error_notification

  .form-inputs
    = f.input :vk_id
    = f.input :image_src
    = f.association :city
    = f.input :text

  .form-actions
    = f.button :submit
